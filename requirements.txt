# Core dependencies
aiohttp>=3.9.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Data fetching
beautifulsoup4>=4.12.0  # For arena.ai scraping
datasets>=2.0.0  # For HF datasets (SWE-bench, LiveCodeBench, EvalPlus, CRUXEval)

# SQLite (built-in)
# No additional package needed

# Optional for development
pytest>=7.0.0
pytest-asyncio>=0.21.0
